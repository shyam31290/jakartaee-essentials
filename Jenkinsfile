pipeline {
    agent any

    stages {
        stage('compile') {
            steps {
                echo "compile the code"
            }
        }
        stage('test') {
            steps {
                echo "test the code"
            }
        }
        stage('package') {
            steps {
                echo "package the code"
            }
        }
    }
}
